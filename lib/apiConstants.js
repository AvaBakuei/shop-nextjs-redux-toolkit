export const ENDPOINTS = {
    // Products
    GET_ALL_PRODUCTS: "/products",
    GET_PRODUCTS_CATEGORIES: "/products/categories",
    GET_PRODUCTS_OF_CATEGORY: (category) => "/products/category/" + category,
}